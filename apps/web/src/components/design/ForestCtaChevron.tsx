/** Chevron for `.ds-forest-cta` — must use `ds-forest-cta__chevron` (see design-studio.css). */
export function ForestCtaChevron() {
  return (
    <svg viewBox="0 0 20 20" className="ds-forest-cta__chevron" fill="none" aria-hidden>
      <path
        d="M8 5l5 5-5 5"
        stroke="currentColor"
        strokeWidth="1.75"
        strokeLinecap="round"
        strokeLinejoin="round"
      />
    </svg>
  );
}
