__pycache__/
*.pyc
.DS_Store
.venv/
.pytest_cache/

# Env and secrets
**/.env
**/.env.local
apps/api/.env

# Node / Next
node_modules/
apps/web/node_modules/
apps/web/.next/
apps/web/out/
*.tsbuildinfo

# Runtime API storage (design packs, SQLite)
apps/api/storage/

# Optional large external dataset
data/zenodo_11khands/

# Documentation (keep locally; do not publish to GitHub)
*.md
docs/

# Local-only folder (backups, moved notes, etc.)
local-only/

# Large / internal assets (not needed on GitHub; keep on disk for local demos)
round1-backup-*.zip
dataset_raw/
plots/
